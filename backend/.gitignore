node_modules
dist

# Keep environment variables out of version control
.env

# Bancos SQLite (dev.db e o test.db dos testes) — gerados, não versionados
*.db
*.db-journal

# Prisma Client gerado (regenerado via `npx prisma generate`)
/generated/prisma
